runtime: nodejs10
api_version: 1

env_variables:
  API_KEY: <API key for projects app engine>

handlers:

  - url: /
    static_files: dist/index.html
    upload: dist/index.html
  - url: /
    static_dir: dist
